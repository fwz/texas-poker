{
  "name": "texas-poker",
  "private": true,
  "workspaces": ["client", "server", "shared"],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,yellow \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "build": "npm run build --workspace=server",
    "build:all": "npm run build --workspace=client && npm run build --workspace=server && npm run test --workspace=server",
    "start": "node server/dist/server/src/index.js",
    "typecheck": "npm run typecheck --workspace=client && npm run typecheck --workspace=server",
    "lint": "npm run lint --workspace=client",
    "test": "npm run test --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
